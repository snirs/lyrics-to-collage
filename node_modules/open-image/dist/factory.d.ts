import execa from 'execa';
declare type OpenImageCommand = {
    command: string;
    parameters: Array<string>;
};
export default function factory(commands: Array<OpenImageCommand>): (imagePath: string) => Promise<execa.ExecaReturns>;
export {};
